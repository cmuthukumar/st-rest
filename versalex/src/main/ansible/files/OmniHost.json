{
 "type": "nativeUser",
 "alias": "{{hostname|upper}}",
 "enabled": true,
 "editable": true,
 "active": true,
  "ready": true,
 "notes": "test notes",
   "home": {
     "dir": {
       "default": "file:{{default_home_dir}}"
   }
}
}
